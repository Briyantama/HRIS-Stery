version: v2

modules:
  - path: .

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX # we use v1 suffix in directory, not package name
  disallow_comment_ignores: true

breaking:
  use:
    - FILE

deps:
  - buf.build/googleapis/googleapis
